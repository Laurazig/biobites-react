const CartPage = () => {

    //useContext

    //useNavigate

    //useState

    // useEffect(() => {})

    // const submitOrder = async (e) => {
    //     e.preventDefault();
    // };

    // const deleteSingleOrderedMeal = (meal) => {
    // }

    // const stripe = async () => {}
    return (
        <div>
            <h2>Cart</h2>
            <h3>Selected meals will show here</h3>
            {/* show meals - delete option */}
            {/* buy now - stripe */}
            
        </div>
    )
}

export default CartPage